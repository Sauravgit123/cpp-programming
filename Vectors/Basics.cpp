#include <iostream>
#include <vector>
using namespace std;

int main()
{
    // dynamic arrays (can be resize..alter the size of arrays)  contiguous m/m allocation

    vector<int> v1; //   OR vector <int> v1(size);
                    // v.size()--> return the length of the vector
                    // v.resize(new size)..change the size of vector
    return 0;
}